/** Proactive alerting: who gets told, when, and how loudly. ScanService (in
 * the job package) fires alerts; this package is everything else --
 * persistence, the rendered delivery record, suppression, and the human
 * decisions (acknowledge, mute) an alert can receive. */
package com.moveinsync.pulse.alert;
