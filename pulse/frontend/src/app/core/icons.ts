import { CheckOutline, EyeOutline, HistoryOutline, ReloadOutline } from '@ant-design/icons-angular/icons';

/**
 * Explicit icon defs registered with ng-zorro (via NzIconModule.forRoot in
 * app.config.ts). Keep this list minimal -- add an icon here only when a
 * component actually renders it with `nz-icon`.
 */
export const PULSE_ICONS = [HistoryOutline, CheckOutline, EyeOutline, ReloadOutline];
